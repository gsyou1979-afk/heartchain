# HeartChain 项目修改意见
## 基于韩国 1365.go.kr 志愿者服务认证标准

---

## 📋 执行摘要

HeartChain 是一个基于区块链的志愿者服务平台，包含：
- **Flutter 手机App** (heartchain/app)
- **NestJS 后台** (heartchain/backend)  
- **Nuxt.js 前台网站** (heartchain/web)
- **智能合约** (heartchain/contracts)

**核心问题**：项目目前不符合韩国 **1365.go.kr** 志愿者服务认证标准，无法进行官方志愿者时间认证。

---

## 🚨 关键问题

### 1. 语言与地区适配问题
**现状**：
- Flutter App UI 使用中文（任务、发布任务、参与等）
- 后台代码注释使用中文（// 技能积分映射、// 单人单次任务）
- 用户表有 `region` 字段但未被正确使用
- 目标市场不明确（中韩混合）

**修改建议**：
```
✅ 统一语言策略：
   - 韩国市场：默认韩语 UI，支持 1365 认证
   - 中国市场：中文 UI，对接 VMS 系统
   - 全球市场：英语 UI
   
✅ 修改文件：
   - /heartchain/app/lib/**/*.dart (所有 UI 文本)
   - /heartchain/web/pages/**/*.vue (所有页面文本)
   - /heartchain/backend/src/**/*.ts (注释和日志)
```

---

### 2. 志愿者时间认证缺失（核心问题）

#### 2.1 当前系统的不足
**现有系统**：
- 使用 HRT 代币作为积分奖励
- 任务状态：pending → assigned → submitted → approved
- 无官方认证号码
- 无官方志愿者时间记录

**1365.go.kr 要求的核心字段**（缺失）：
| 必需字段 | 当前状态 | 优先级 |
|---------|---------|--------|
| 志愿者认证号码 (봉사인증번호) | ❌ 缺失 | 🔴 最高 |
| 官方志愿者时间 (봉사시간) | ❌ 缺失 (仅有 HRT 积分) | 🔴 最高 |
| 活动日期时间 (활동일시) | ⚠️ 有 schedule 但不规范 | 🟠 高 |
| 认证机构代码 (기관코드) | ❌ 缺失 | 🔴 最高 |
| 确认者信息 (확인자) | ❌ 缺失 | 🟠 高 |
| 志愿者注册号 (봉사자등록번호) | ❌ 缺失 | 🟠 高 |

#### 2.2 后台实体修改建议

**修改文件**：`/heartchain/backend/src/tasks/entities/task.entity.ts`

```typescript
// 新增字段（符合 1365 标准）
@Entity('tasks')
export class Task {
  // ... 现有字段 ...
  
  // ⭐ 1365 认证必需字段 ⭐
  
  @Column({ type: 'varchar', length: 20, nullable: true, comment: '1365认证号码' })
  certificationNumber?: string;
  
  @Column({ type: 'decimal', precision: 4, scale: 1, default: 0, comment: '官方认证志愿者时间(小时)' })
  certifiedVolunteerHours: number;
  
  @Column({ type: 'varchar', length: 10, nullable: true, comment: '机构代码' })
  organizationCode?: string;  // 1365 机构代码
  
  @Column({ type: 'varchar', length: 100, nullable: true, comment: '确认者姓名' })
  verifiedByName?: string;
  
  @Column({ type: 'varchar', length: 50, nullable: true, comment: '确认者职务' })
  verifiedByTitle?: string;
  
  @Column({ type: 'timestamptz', nullable: true, comment: '活动开始时间' })
  activityStartTime?: Date;
  
  @Column({ type: 'timestamptz', nullable: true, comment: '活动结束时间' })
  activityEndTime?: Date;
  
  @Column({ type: 'boolean', default: false, comment: '是否已同步到1365系统' })
  syncedTo1365: boolean;
  
  @Column({ type: 'timestamp', nullable: true, comment: '1365同步时间' })
  syncedAt?: Date;
  
  // 区分奖励积分与官方时间
  @Column({ type: 'int', default: 0, comment: 'HRT代币奖励（非官方时间）' })
  pointsReward: number;  // 保持现有字段
}
```

**修改文件**：`/heartchain/backend/src/users/entities/user.entity.ts`

```typescript
@Entity('users')
export class User {
  // ... 现有字段 ...
  
  // ⭐ 1365 认证必需字段 ⭐
  
  @Column({ type: 'varchar', length: 20, nullable: true, comment: '志愿者注册号码' })
  volunteerRegNumber?: string;  // 1365 志愿者注册号
  
  @Column({ type: 'varchar', length: 20, nullable: true, comment: '身份证号(加密存储)' })
  idCardEncrypted?: string;  // 需加密
  
  @Column({ type: 'decimal', precision: 6, scale: 1, default: 0, comment: '累计官方志愿者时间' })
  totalCertifiedHours: number;
  
  @Column({ type: 'boolean', default: false, comment: '是否通过1365实名认证' })
  verified1365: boolean;
  
  @Column({ type: 'varchar', length: 10, nullable: true, comment: '年龄分组：youth/adult' })
  ageGroup?: string;  // 青少年限制适用
  
  // 保留现有字段（用于内部积分系统）
  @Column({ type: 'decimal', precision: 18, scale: 6, default: 0, comment: 'HRT代币余额' })
  pointBalance: number;
}
```

---

### 3. 青少年志愿者时间限制（韩国法律要求）

根据 1365 标准：
- **青少年（19岁以下）**：每日最多 4~6 小时
- **成人**：每日最多 8 小时
- **违规处理**：超额时间不予认证

**修改建议**：

修改文件：`/heartchain/backend/src/tasks/tasks.service.ts`

```typescript
// 添加验证方法
async validateVolunteerHours(userId: string, taskId: string, hours: number): Promise<boolean> {
  const user = await this.usersService.findById(userId);
  const task = await this.findById(taskId);
  
  // 检查用户年龄组
  const maxHours = user.ageGroup === 'youth' ? 6 : 8;
  
  // 检查当日已认证时间
  const todayHours = await this.getTodayCertifiedHours(userId);
  
  if (todayHours + hours > maxHours) {
    throw new BadRequestException(
      `今日可认证时间超限。${user.ageGroup === 'youth' ? '青少年' : '成人'}每日最多 ${maxHours} 小时`
    );
  }
  
  return true;
}
```

---

### 4. 1365.go.kr API 集成（关键功能）

**需要实现的功能**：

#### 4.1 创建 1365 服务模块
新建文件：`/heartchain/backend/src/volunteer-certification/volunteer-1365.service.ts`

```typescript
@Injectable()
export class Volunteer1365Service {
  private readonly API_BASE = 'https://www.1365.go.kr/api/v1';
  
  // 同步志愿者时间到 1365
  async syncVolunteerHours(data: {
    volunteerRegNumber: string;
    organizationCode: string;
    activityDate: Date;
    hours: number;
    activityContent: string;
  }): Promise<{ certificationNumber: string }> {
    // 调用 1365 API
    const response = await fetch(`${this.API_BASE}/certify`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${process.env.VOLUNTEER_1365_API_KEY}`,
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(data),
    });
    
    if (!response.ok) {
      throw new Error('1365 同步失败');
    }
    
    return response.json();
  }
  
  // 查询志愿者认证记录
  async getCertificationHistory(volunteerRegNumber: string) {
    // 实现查询逻辑
  }
}
```

#### 4.2 修改任务完成流程
修改文件：`/heartchain/backend/src/tasks/tasks.service.ts`

```typescript
async completeTask(taskId: string, verifierId: string) {
  const task = await this.findById(taskId);
  
  // 1. 验证志愿者时间是否合规
  await this.validateVolunteerHours(task.assigneeId, taskId, task.certifiedVolunteerHours);
  
  // 2. 同步到 1365 系统
  try {
    const certResult = await this.volunteer1365Service.syncVolunteerHours({
      volunteerRegNumber: task.assignee.volunteerRegNumber,
      organizationCode: task.organizationCode,
      activityDate: task.activityStartTime,
      hours: task.certifiedVolunteerHours,
      activityContent: task.title,
    });
    
    // 3. 更新任务认证信息
    task.certificationNumber = certResult.certificationNumber;
    task.syncedTo1365 = true;
    task.syncedAt = new Date();
    
  } catch (error) {
    // 记录同步失败，但不阻止任务完成
    console.error('1365 同步失败:', error);
    task.syncedTo1365 = false;
  }
  
  // 4. 发放 HRT 代币奖励（非官方时间）
  await this.pointsService.rewardUser(task.assigneeId, task.pointsReward);
  
  // 5. 更新用户总认证时间
  await this.usersService.addCertifiedHours(task.assigneeId, task.certifiedVolunteerHours);
  
  return task.save();
}
```

---

### 5. 前台 App 修改建议

#### 5.1 Flutter App (heartchain/app)

**修改文件**：`/heartchain/app/lib/features/tasks/tasks_page.dart`

```dart
// 添加认证状态显示
Widget _buildCertificationBadge(TaskModel task) {
  if (task.certificationNumber != null) {
    return Container(
      padding: EdgeInsets.symmetric(horizontal: 8, vertical: 4),
      decoration: BoxDecoration(
        color: Colors.green.withOpacity(0.1),
        borderRadius: BorderRadius.circular(8),
      ),
      child: Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          Icon(Icons.verified, size: 14, color: Colors.green),
          SizedBox(width: 4),
          Text(
            '1365 인증완료',  // 韩语：1365认证完成
            style: TextStyle(color: Colors.green, fontSize: 12),
          ),
        ],
      ),
    );
  }
  return SizedBox.shrink();
}
```

**修改文件**：`/heartchain/app/lib/features/profile/profile_page.dart`

```dart
// 添加官方认证时间显示（区别于 HRT 积分）
Widget _buildStatsCard(UserModel user) {
  return Card(
    child: Padding(
      padding: EdgeInsets.all(16),
      child: Column(
        children: [
          // 官方志愿者时间
          _statRow('공식 봉사시간', '${user.totalCertifiedHours}시간', Icons.verified),
          Divider(),
          // HRT 代币积分
          _statRow('HRT 포인트', '${user.totalPoints} HRT', Icons.token),
        ],
      ),
    ),
  );
}
```

#### 5.2 Web 前台 (heartchain/web)

**修改文件**：`/heartchain/web/pages/tasks/index.vue`

```vue
<template>
  <!-- 添加认证标签 -->
  <div v-if="task.certificationNumber" class="badge bg-green-100 text-green-700">
    ✅ 1365 인증완료 ({{ task.certifiedVolunteerHours }}시간)
  </div>
  
  <!-- 区分显示 -->
  <div class="grid grid-cols-2 gap-4 mt-4">
    <div class="bg-blue-50 p-3 rounded">
      <div class="text-sm text-gray-600">공식 봉사시간</div>
      <div class="text-xl font-bold text-blue-600">{{ task.certifiedVolunteerHours }}시간</div>
    </div>
    <div class="bg-red-50 p-3 rounded">
      <div class="text-sm text-gray-600">HRT 리워드</div>
      <div class="text-xl font-bold text-red-600">{{ task.pointsReward }} HRT</div>
    </div>
  </div>
</template>
```

---

### 6. 数据隐私与合规（韩国 PIPA 法）

**问题**：
- `idCard` 字段明文存储（违反隐私法）
- 无年龄验证机制
- 无数据删除功能（GDPR/PIPA 要求）

**修改建议**：

```typescript
// 修改：/heartchain/backend/src/users/entities/user.entity.ts
import * as crypto from 'crypto';

@Entity('users')
export class User {
  // ... 其他字段 ...
  
  @Column({ type: 'varchar', length: 500, nullable: true, comment: '身份证号(加密)' })
  idCardEncrypted?: string;
  
  // 加密方法
  set idCard(value: string) {
    if (value) {
      const cipher = crypto.createCipher('aes-256-gcm', process.env.ID_CARD_SECRET);
      this.idCardEncrypted = cipher.update(value, 'utf8', 'hex') + cipher.final('hex');
    }
  }
  
  // 添加数据删除方法（PIPA 合规）
  async deletePersonalData() {
    this.phone = '';
    this.idCardEncrypted = '';
    this.realName = '';
    this.status = UserStatus.INACTIVE;
    // 保留匿名统计数据
  }
}
```

---

### 7. 任务创建表单修改

**修改文件**：`/heartchain/web/pages/tasks/index.vue` (发布任务表单)

```vue
<script setup>
const form = reactive({
  // ... 现有字段 ...
  
  // ⭐ 新增 1365 认证相关字段 ⭐
  organizationCode: '',        // 机构代码
  activityStartTime: '',       // 活动开始时间
  activityEndTime: '',         // 活动结束时间
  certifiedVolunteerHours: 0,  // 官方认证时间
  verificationMethod: '1365',   // 认证方式：1365 / internal
});
</script>

<template>
  <!-- 添加 1365 认证设置 -->
  <div class="bg-blue-50 p-4 rounded-lg mb-6">
    <h3 class="font-semibold mb-3">🏛️ 1365 인증 설정</h3>
    
    <div class="grid grid-cols-2 gap-4">
      <div>
        <label>기관코드 (1365)</label>
        <input v-model="form.organizationCode" class="input-field" 
               placeholder="1365 기관코드 입력" />
      </div>
      
      <div>
        <label>공식 봉사시간 (시간)</label>
        <input v-model="form.certifiedVolunteerHours" type="number" 
               class="input-field" min="0.5" max="8" step="0.5" />
        <small class="text-gray-500">성인: 최대 8시간/일, 청소년: 최대 6시간/일</small>
      </div>
      
      <div>
        <label>활동 시작시간</label>
        <input v-model="form.activityStartTime" type="datetime-local" class="input-field" />
      </div>
      
      <div>
        <label>활동 종료시간</label>
        <input v-model="form.activityEndTime" type="datetime-local" class="input-field" />
      </div>
    </div>
    
    <div class="mt-3 p-3 bg-yellow-50 rounded border border-yellow-200">
      ⚠️ 1365 인증이 가능한 기관만 작성해주세요. 허위 인증 시 법적 책임이 있습니다.
    </div>
  </div>
  
  <!-- HRT 리워드 (可选，非官方) -->
  <div class="bg-red-50 p-4 rounded-lg">
    <h3 class="font-semibold mb-3">🎁 HRT 리워드 (선택사항)</h3>
    <p class="text-sm text-gray-600 mb-2">
      블록체인 토큰 보상 (공식 봉사시간과 별도)
    </p>
    <input v-model="form.pointsReward" type="number" class="input-field" 
           placeholder="HRT 토큰 수량" />
  </div>
</template>
```

---

## 📁 需要修改的文件清单

### 后台 (Backend - NestJS)
| 文件路径 | 修改内容 | 优先级 |
|---------|---------|--------|
| `backend/src/tasks/entities/task.entity.ts` | 添加 1365 认证字段 | 🔴 最高 |
| `backend/src/users/entities/user.entity.ts` | 添加志愿者注册号、认证时间、隐私加密 | 🔴 最高 |
| `backend/src/tasks/tasks.service.ts` | 添加 1365 同步逻辑、时间验证 | 🔴 最高 |
| `backend/src/tasks/tasks.controller.ts` | 添加认证相关 API | 🟠 高 |
| `backend/src/users/users.service.ts` | 添加隐私数据管理 | 🟠 高 |
| **新建** `backend/src/volunteer-certification/volunteer-1365.service.ts` | 1365 API 集成 | 🔴 最高 |
| **新建** `backend/src/volunteer-certification/volunteer-1365.controller.ts` | 1365 相关接口 | 🟠 高 |

### 手机 App (Flutter)
| 文件路径 | 修改内容 | 优先级 |
|---------|---------|--------|
| `app/lib/features/tasks/tasks_page.dart` | 添加认证状态显示、韩语 UI | 🟠 高 |
| `app/lib/features/profile/profile_page.dart` | 显示官方时间 vs HRT 积分 | 🟠 高 |
| `app/lib/shared/models/models.dart` | 添加新字段模型 | 🔴 最高 |
| `app/lib/features/tasks/create_task_page.dart` | 添加 1365 认证设置 | 🟠 高 |

### 前台网站 (Nuxt.js)
| 文件路径 | 修改内容 | 优先级 |
|---------|---------|--------|
| `web/pages/tasks/index.vue` | 添加认证标签、修改发布表单 | 🟠 高 |
| `web/pages/profile/index.vue` | 显示官方认证时间 | 🟠 高 |
| `web/components/task/TaskCard.vue` | 显示认证徽章 | 🟡 中 |

---

## 🎯 实施路线图

### Phase 1: 核心认证功能（1-2周）
1. 修改后台实体（Task + User）
2. 创建 `volunteer-1365.service.ts`
3. 实现 1365 API 集成
4. 修改任务完成流程

### Phase 2: 前台界面适配（1周）
1. Flutter App 添加认证状态显示
2. Web 前台修改表单和显示
3. 多语言支持（韩语 UI）

### Phase 3: 合规与优化（1周）
1. 数据加密与隐私保护
2. 青少年时间限制验证
3. 测试与上线

---

## 📊 对比：当前系统 vs 1365 标准

| 功能 | 当前 HeartChain | 1365.go.kr 标准 | 符合度 |
|-----|----------------|----------------|-------|
| 官方认证号码 | ❌ 无 | ✅ 必需 | 0% |
| 官方志愿者时间 | ❌ 仅有 HRT 积分 | ✅ 必需 | 0% |
| 机构代码 | ❌ 无 | ✅ 必需 | 0% |
| 青少年限制 | ❌ 无 | ✅ 法定要求 | 0% |
| VMS 集成 | ❌ 无 | ✅ 推荐 | 0% |
| 隐私保护 | ⚠️ 不足 | ✅ PIPA 合规 | 30% |
| 区块链积分 | ✅ 有 | ❌ 非必需 | N/A (额外功能) |

---

## 💡 额外建议

1. **区分两种奖励系统**：
   - **官方志愿者时间**：用于升学、就业认证（与 1365 同步）
   - **HRT 代币**：平台内部激励（非官方，可保留）

2. **考虑 VMS 集成**：
   - 韩国还有 VMS (Volunteer Management System, vms.or.kr)
   - 两者可同时支持

3. **测试建议**：
   - 先在测试环境申请 1365 API 测试账号
   - 与当地志愿者中心合作测试

4. **法律咨询**：
   - 建议咨询韩国律师事务所确保 PIPA 合规
   - 特别是青少年数据处理

---

## 📞 联系方式

如需进一步讨论或修改代码，请联系开发团队。

**生成时间**：2026-04-29
**分析版本**：HeartChain v1.0
**参考标准**：1365.go.kr 志愿者认证规范（2026版）
